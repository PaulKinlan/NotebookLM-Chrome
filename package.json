{
  "name": "foliolm",
  "version": "0.1.0",
  "description": "FolioLM - Collect and query content from tabs, bookmarks, and history. Your AI research companion.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "watch": "npm run build -- --watch",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix",
    "typecheck": "tsc --noEmit",
    "package:zip": "cd dist && zip -r ../foliolm-extension-v${npm_package_version}.zip .",
    "package": "npm run package:zip",
    "release:patch": "npm version patch --no-git-tag-version && npm run build && npm run package && git commit -am \"chore(release): v$npm_package_version\" && git tag v$npm_package_version",
    "release:minor": "npm version minor --no-git-tag-version && npm run build && npm run package && git commit -am \"chore(release): v$npm_package_version\" && git tag v$npm_package_version",
    "release:major": "npm version major --no-git-tag-version && npm run build && npm run package && git commit -am \"chore(release): v$npm_package_version\" && git tag v$npm_package_version"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.7",
    "@ai-sdk/google": "^3.0.5",
    "@ai-sdk/groq": "^3.0.4",
    "@ai-sdk/huggingface": "^1.0.4",
    "@ai-sdk/openai": "^3.0.7",
    "@ai-sdk/openai-compatible": "^2.0.4",
    "@built-in-ai/core": "^3.0.1",
    "@mozilla/readability": "^0.6.0",
    "@openrouter/ai-sdk-provider": "^1.5.4",
    "@types/marked": "^5.0.2",
    "ai": "^6.0.14",
    "dompurify": "^3.3.1",
    "marked": "^17.0.1",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.287",
    "@types/dompurify": "^3.0.5",
    "@types/mozilla__readability": "^0.4.2",
    "@types/node": "^25.0.5",
    "@types/turndown": "^5.0.5",
    "@typescript-eslint/eslint-plugin": "^8.19.0",
    "@typescript-eslint/parser": "^8.19.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.6"
  },
  "overrides": {
    "@openrouter/ai-sdk-provider": {
      "ai": "^6.0.14"
    }
  }
}
